//Template del Store a utilizar. Se recomienda comentar las variables que pueden estar asignadas en cada estado
const initialState = {
  //usuario: null
};

const rootReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default rootReducer;
